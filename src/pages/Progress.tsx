import React, {FC} from "react";
import {Text, SafeAreaView} from "react-native";

export const Progress: FC = () => (
    <SafeAreaView>
        <Text>{'This will be the PROGRESS page.'}</Text>
    </SafeAreaView>
);
